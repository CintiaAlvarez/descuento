/* let alumno = prompt("Ingrese nombre del alumno");
let nota1, nota2, nota3, promedio;

while (alumno !== "--") {
  nota1 = Number(prompt("ingrese nota práctica"));
  nota2 = Number(prompt("ingrese nota teórica"));
  nota3 = Number(prompt("ingrese nota problemas"));

  if (
    nota1 <= 10 &&
    nota1 >= 0 &&
    nota2 <= 10 &&
    nota2 >= 0 &&
    nota3 <= 10 &&
    nota3 >= 0
  ) {
    promedio = nota1 * 0.1 + nota2 * 0.4 + nota3 * 0.5;

    console.log("El promedio de ", alumno, "es", promedio);
  } else {
    console.log("Ingresar un dato válido");

    alumno = prompt("ingrese nombre del alumno");
  }
}
*/
